<mah:MetroWindow x:Class="WpfMrpSimulatorApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm ="clr-namespace:WpfMrpSimulatorApp.ViewModels"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconpacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:WpfMrpSimulatorApp.Views"
        mc:Ignorable="d"
        Title="Mrp Simulator" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="7*"></RowDefinition>
        </Grid.RowDefinitions>
        <!--ui구성-->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button  Content="Schedule" Command="{Binding ScheduleCommand}" Margin="0,0,10,0"></Button>
            <Button  Content="Monitoring" Command="{Binding MonitoringCommand}" Margin="0,0,10,0"></Button>
            <Button  Content="Analysis" Command="{Binding AnalysisCommand}" Margin="0,0,10,0"></Button>
            <Button  Content="Setting"  Command="{Binding SettingCommand}"></Button>
        </StackPanel>

        <ContentControl Grid.Row="1"  Content="{Binding CurrentViewModel}" />
    </Grid>
</mah:MetroWindow>
